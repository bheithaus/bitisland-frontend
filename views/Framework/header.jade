nav.navbar.navbar-default.navbar-inverse.navbar-fixed-top(role='navigation')
  div.container
    // Brand and toggle get grouped for better mobile display
    div.navbar-header
      button.navbar-toggle(type='button', data-toggle='collapse', data-target='#mobile-header')
        span.sr-only Toggle navigation
        span.icon-bar
        span.icon-bar
        span.icon-bar

      a.navbar-brand(href='/')
        img(src='/logo.png')

    // Collect the nav links, forms, and other content for toggling
    div#mobile-header.collapse.navbar-collapse
      ul.nav.navbar-nav
        li
          a(href='/trade') The Net's Safest Maker Taker ECN

      ul.nav.navbar-nav.navbar-right
        li(ng-show="loggedIn()")
          a.btn.btn-default.theme(ng-click='toggleColor()', ng-class='color') theme
        
        li(ng-hide="loggedIn()")
          a.btn.btn-success(ng-click="register()") Sign Up

        li(ng-hide="loggedIn()")
          a(ng-click="login()") Login

        li(ng-show="loggedIn()")
          a(ng-click="logout()") Logout